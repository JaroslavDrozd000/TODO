export enum LABEL_ENUM {
  school = 'School',
  job = 'Job',
  personal = 'Personal',
}

export enum PRIORITY_ENUM {
  low = 'low',
  medium = 'medium',
  high = 'high',
}

export enum STATUS_ENUM {
  toDo = 'toDo',
  inProgress = 'inProgress',
  done = 'done',
}
